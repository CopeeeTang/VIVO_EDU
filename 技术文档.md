todo list
后端：
1. 从数据库/中间json数据提取，调用figure_conflict.ipynb，生成图表，最后将生成的图表放入历史数据页面进行展示 （待测试
2. 处理时间优化（待定）  
3. 删除一些无用报告分析  

前端：
1. 文件历史页面修改为问卷收集页面  
2. 主页面分析进度条的优化        （doing
3. 删除报告（与后端数据库串联）
4. 将查看报告这一步骤加到分析之中，使得生成完能直接查看   （完成

最后：
整合所有功能，进行手机端Google适配

加入报错终止，比如没有生成报告，或者没有生成图表


# 家庭教育AI辅助系统技术说明文档

## 1. 系统架构

### 1.1 整体架构

本系统采用客户端-服务器架构，主要包括以下组件：

1. 移动应用（iOS/Android）
2. Web 服务器
3. 数据库
4. LLM（大型语言模型）分析服务
5. 异步任务处理服务

## 1.2 系统工作流程

### 1.2.1 用户认证
1. 用户输入：用户名和密码
2. 前端发送：POST请求到 `/api/auth/login`
3. 服务器处理：
   - 查询Users表：SELECT * FROM Users WHERE username = ?
   - 验证密码，生成JWT token
4. 服务器响应：返回JWT token和用户ID

### 1.2.2 音频文件上传
1. 用户操作：选择或录制音频文件
2. 前端发送：GET请求到 `/api/audio/upload-url`
3. 服务器处理：
   - 生成安全的上传URL和file_id
   - 在AudioFiles表预创建记录：INSERT INTO AudioFiles (file_id, user_id, file_name, status) VALUES (?, ?, ?, 'pending')
4. 服务器响应：返回上传URL和file_id
5. 前端处理：使用返回的URL上传文件到云存储
6. 上传完成后，前端发送：POST请求到 `/api/audio/upload-complete`
7. 服务器处理：
   - 更新AudioFiles表：UPDATE AudioFiles SET status = 'processing', progress = 0 WHERE file_id = ?
   - 触发异步任务处理音频文件

### 1.2.3 音频转写和分析
1. 服务器接收上传完成请求后，启动异步任务处理：
   - 调用异步任务函数 `process_audio_task` 进行音频转写和分析
2. 异步任务处理：
   - 调用语音识别API进行转写
   - 调用LLM服务进行分析
   - 更新处理进度并保存分析结果
3. 结果存储：
   - 创建AnalysisResults记录：
     ```sql
     INSERT INTO AnalysisResults (file_id, transcript, education_style, sentiment_analysis, communication_patterns, topics, conflict_analysis, advantages, scenarios, intervention_strategy) 
     VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
     ```
   - 更新AudioFiles状态：
     ```sql
     UPDATE AudioFiles SET status = 'completed', progress = 100 WHERE file_id = ?
     ```

### 1.2.4 获取分析结果
1. 用户操作：选择查看特定音频的分析结果
2. 分析结果包括两大部分：一部分是特征提取，一部分是评估及干预   
    特征提取：
     1. 情感分析（包括情绪变化曲线图和情绪评估能力两部分）
     2. 沟通模式分析（积极、中性、消极行为分布）
     3. 场景冲突分析（冲突类型分布及具体冲突分析）
    评估及干预 
     1. 优势与劣势
     2. 对话及改善后的对话，包括原因
     3. 家庭教育风格分析
     4. 引起家长反思的两个问题
     5. 具体干预策略

3. 前端发送：GET请求到 `/api/audio/analysis/<file_id>`
4. 服务器处理：
   - 查询AnalysisResults表：
     ```sql
     SELECT * FROM AnalysisResults WHERE file_id = ?
     ```
5. 服务器响应：返回完整的分析结果数据

### 1.2.5 实时状态更新
1. 前端发送：GET请求到 `/api/audio/status/<file_id>`
2. 服务器处理：
   - 查询AudioFiles表：
     ```sql
     SELECT status, progress FROM AudioFiles WHERE file_id = ?
     ```
3. 服务器响应：返回当前处理状态和进度

### 1.2.6 历史记录和趋势分析
1. 用户操作：查看历史记录或趋势分析
2. 前端发送：GET请求到 `/api/audio/history` 
3. 服务器处理：
   - 查询AnalysisResults表：
     ```sql
     SELECT * FROM AnalysisResults WHERE user_id = ? ORDER BY created_at DESC LIMIT ?
     ```
   - 对多次分析结果进行汇总和计算
   - 调用 `figure_conflict` 生成图表
4. 服务器响应：返回汇总后的历史数据、趋势数据及生成的图表
```


## 2. 数据库设计

### 2.1 用户表(Users)

| 字段名     | 类型        | 说明                   |
|------------|-------------|------------------------|
| user_id    | INTEGER     | 主键，用户唯一标识     |
| username   | VARCHAR(50) | 用户名，唯一且不可为空 |
| password   | VARCHAR(255)| 加密后的密码，不可为空 |
| created_at | TIMESTAMP   | 创建时间，默认当前时间 |
| last_login | TIMESTAMP   | 最后登录时间，自动更新 |

### 2.2 音频文件表(AudioFiles)

| 字段名       | 类型          | 说明                                      |
|--------------|---------------|-------------------------------------------|
| file_id      | VARCHAR(36)   | 主键，UUID格式文件唯一标识                |
| user_id      | INTEGER       | 外键，关联用户表，不可为空                |
| file_name    | VARCHAR(255)  | 文件名，不可为空                          |
| custom_name  | VARCHAR(255)  | 自定义名称，可为空                        |
| file_path    | VARCHAR(255)  | 文件存储路径，不可为空                    |
| duration     | FLOAT         | 音频时长（秒），可为空                    |
| created_at   | TIMESTAMP     | 上传时间，默认当前时间                    |
| status       | ENUM          | 处理状态（pending/processing/completed/failed） |
| task_id      | VARCHAR(50)   | 任务ID，可为空                            |
| progress     | INTEGER       | 处理进度，默认0，不可为空                |

### 2.3 转写文本表(Transcriptions)

| 字段名           | 类型        | 说明                            |
|------------------|-------------|---------------------------------|
| transcription_id | INTEGER     | 主键，转写唯一标识              |
| transcript       | TEXT        | 完整转录文本，不可为空          |
| file_id          | VARCHAR(36) | 外键，关联音频文件表，不可为空  |
| content          | TEXT        | 转写内容，不可为空              |

### 2.4 分析结果表(AnalysisResults)

| 字段名                     | 类型          | 说明                                   |
|----------------------------|---------------|----------------------------------------|
| analysis_id                | INTEGER       | 主键，分析结果唯一标识                 |
| file_id                    | VARCHAR(36)   | 外键，关联音频文件表，级联删除，不可为空|
| family_overview            | TEXT          | 家庭教育风格分析                       |
| conversation_analysis      | TEXT          | 沟通分析                               |
| emotion_analysis_result    | TEXT          | 情绪分析                               |
| reflection_questions       | TEXT          | 反思问题                               |
| conflict_analysis_result   | TEXT          | 冲突分析                               |
| behavior_analysis_result   | TEXT          | 行为分析                               |
| strategy_generation_result | TEXT          | 策略生成                               |
| created_at                 | TIMESTAMP     | 分析时间，默认当前时间                 |
```

## 3. 服务器API设计

### 3.1 用户登录(优先级:P0)

#### 说明
验证用户身份并返回登录token

#### URL
`POST http://api.example.com/v1/user/login`

#### 请求示例

json
{
"username": "test_user",
"password": "hashed_password"
}

#### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| username | 用户名 | String | 是 |
| password | 加密后的密码 | String | 是 |

#### 返回数据

json
{
"code": 200,
"msg": "登录成功",
"data": {
"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
"user_id": 12345
}
}

### 3.2 上传音频文件(优先级:P0)

#### 说明
用户上传音频文件到服务器

#### URL
`POST http://api.example.com/v1/audio/upload`

#### 请求头
Content-Type: multipart/form-data
Authorization: Bearer <token>


#### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| audio_file | 音频文件 | File | 是 |
| file_name | 文件名 | String | 是 |

#### 返回数据

json
{
"code": 200,
"msg": "上传成功",
"data": {
"file_id": 67890,
"file_name": "recording_20230601.mp3",
"file_path": "/storage/audio/67890.mp3"
}
}


### 3.3 获取分析结果(优先级:P1)

#### 3.3.1 获取音频转录文本(优先级:P0)

##### 说明
获取指定音频文件的转录文本

##### URL
`GET http://api.example.com/v1/transcription/<file_id>`

##### 请求头
Authorization: Bearer <token>

##### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| file_id | 音频文件ID | Integer | 是 |

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": [
    {
      "id": 1,
      "speaker": "child",
      "start_time": "00:00:00.210000",
      "end_time": "00:00:01.730000",
      "content": "到。 嗯嗯"
    },
    {
      "id": 2,
      "speaker": "parent",
      "start_time": "00:00:03.560000",
      "end_time": "00:00:07.560000",
      "content": "那现在检查一下呀，过来。 嗯老师。"
    },
    {
      "id": 3,
      "speaker": "child",
      "start_time": "00:00:08.280000",
      "end_time": "00:00:09.850000",
      "content": "对陈老师。 什么？"
    },
    {
      "id": 4,
      "speaker": "parent",
      "start_time": "00:00:22.800000",
      "end_time": "00:00:33.050000",
      "content": "你过来，兄弟看。 注意看。 过来，检查一下你的作业，啊"
    },
    {
      "id": 5,
      "speaker": "parent",
      "start_time": "00:00:43.050000",
      "end_time": "00:00:44.540000",
      "content": "你语文写的什么？呀"
    }
  ]
}
```

#### 3.3.2 获取家庭教育风格分析(优先级:P1)

##### 说明
获取指定音频文件的家庭教育风格分析结果

##### URL
`GET http://api.example.com/v1/analysis/family-overview/<file_id>`

##### 请求头
Authorization: Bearer <token>

##### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| file_id | 音频文件ID | Integer | 是 |

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "family_overview": "# 🔍揭秘您的家庭教育风格：亮点与潜力\n\n..."
  }
}
```

#### 3.3.3 获取沟通分析(优先级:P1)

##### 说明
获取指定音频文件的沟通分析结果

##### URL
`GET http://api.example.com/v1/analysis/conversation-analysis/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "conversation_analysis": "# 🗣️沟通魔法：如何与孩子建立深度连接\n\n..."
  }
}
```

#### 3.3.4 获取情绪分析(优先级:P1)

##### 说明
获取指定音频文件的情绪分析结果

##### URL
`GET http://api.example.com/v1/analysis/emotion-analysis/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "emotion_analysis_result": "# 🌡️ 情绪温度计：追踪亲子互动中的情感波动\n\n..."
  }
}
```

#### 3.3.5 获取反思问题(优先级:P1)

##### 说明
获取指定音频文件的反思问题

##### URL
`GET http://api.example.com/v1/analysis/reflection-questions/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "reflection_questions": "# 🪞 心灵反思镜：您可以试着反思\n\n..."
  }
}
```

#### 3.3.6 获取冲突分析(优先级:P1)

##### 说明
获取指定音频文件的冲突分析结果

##### URL
`GET http://api.example.com/v1/analysis/conflict-analysis/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "conflict_analysis_result": "# 🤝 冲突解决：如何处理家庭作业辅导中的冲突\n\n..."
  }
}
```

#### 3.3.7 获取行为分析(优先级:P1)

##### 说明
获取指定音频文件的行为分析结果

##### URL
`GET http://api.example.com/v1/analysis/behavior-analysis/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "behavior_analysis_result": "# 🎬 冲突舞台：还原您和孩子间的真实一幕\n\n..."
  }
}
```

#### 3.3.8 获取策略生成结果(优先级:P1)

##### 说明
获取指定音频文件的策略生成结果

##### URL
`GET http://api.example.com/v1/analysis/strategy-generation/<file_id>`

##### 请求头和请求参数
(与3.3.2相同)

##### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "analysis_id": 54321,
    "strategy_generation_result": "# 🎯量身定制的亲子秘籍：让每次对话更有效\n\n..."
  }
}
```

### 3.4 获取历史轨迹(优先级:P2)

#### 说明
获取用户的历史使用轨迹和进度

#### URL
`GET http://api.example.com/v1/user/history`

#### 请求头

Authorization: Bearer <token>


#### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| start_date | 开始日期 | Date | 否 |
| end_date | 结束日期 | Date | 否 |

#### 返回数据

json
{
"code": 200,
"msg": "获取成功",
"data": {
"total_recordings": 15,
"total_duration": 7200,
"analysis_count": 12,
"improvement_rate": 0.75,
"timeline": [
{
"date": "2023-06-01",
"recordings": 2,
"duration": 600,
"main_topics": ["数学", "英语"]
},
// ... 更多历史数据
]
}
}

### 3.5 获取音频文件上传URL(优先级:P0)

#### 说明
获取安全的音频文件上传URL

#### URL
`GET http://api.example.com/v1/audio/upload-url`

#### 请求头
Authorization: Bearer <token>

#### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| file_name | 文件名 | String | 是 |
| file_size | 文件大小(字节) | Integer | 是 |

#### 返回数据

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "upload_url": "https://storage.example.com/upload/67890",
    "file_id": 67890
  }
}
```

### 3.6 确认音频文件上传完成(优先级:P0)

#### 说明
通知服务器音频文件上传已完成，触发处理流程

#### URL
`POST http://api.example.com/v1/audio/upload-complete`

#### 请求头
Authorization: Bearer <token>

#### 请求参数

| 参数名称 | 参数说明 | 类型 | 是否必须 |
|----------|----------|------|----------|
| file_id | 文件ID | Integer | 是 |

#### 返回数据

```json
{
  "code": 200,
  "msg": "确认成功",
  "data": {
    "status": "processing"
  }
}
```

## 4. 客户端功能设计

### 4.1 音频录制和上传

- 实现音频录制功能，支持暂停和继续
- 提供音频文件选择功能
- 实现音频文件的安全上传，使用服务器提供的安全URL
- 上传完成后通知服务器触发处理流程

### 4.2 音频文件管理

- 实现音频文件列表的展示和管理
- 提供音频文件的删除功能
- 显示音频文件的处理状态（待处理/处理中/已完成）

### 4.3 分析结果展示

- 设计并实现分析结果的可视化界面
- 提供历史数据的趋势图和进度报告
- 实现实时更新分析结果的功能（可考虑使用WebSocket或轮询）

### 4.4 用户认证和授权

- 实现用户登录和注销功能
- 管理用户会话和访问令牌
- 实现访问令牌的自动刷新机制

## 5. 安全性考虑

### 5.1 数据传输安全

- 使用HTTPS协议确保所有API通信的安全
- 实现文件上传的安全机制，如使用预签名URL或临时凭证

### 5.2 用户认证

- 实现基于JWT的用户认证机制
- 在客户端安全存储访问令牌，定期刷新

### 5.3 权限控制

- 实现细粒度的权限控制，确保用户只能访问自己的数据
- 在服务器端进行权限验证，防止未授权访问

## 6. 性能优化

### 6.1 客户端缓存

- 实现适当的缓存机制，减少不必要的网络请求
- 缓存不敏感的静态数据，如UI资源和配置信息

### 6.2 大文件上传优化

- 实现分片上传功能，支持大文件的可靠传输
- 提供断点续传功能，优化网络不稳定情况下的上传体验

### 6.3 响应式设计

- 优化UI渲染性能，确保流畅的用户体验
- 实现懒加载和分页加载，优化长列表的展示

## 7. 注意事项

1. 所有API请求都需要进行身份验证，除了登录接口。
2. 音频文件上传应考虑大文件传输的优化，如分片上传和断点续传。
3. 分析结果的获取可能需要异步处理，考虑使用WebSocket或轮询机制。
4. 数据库设计时需考虑数据量增长带来的性能影响，适时进行分表或分库。
5. API返回的错误信息应统一格式，便于前端处理。
6. 考虑实现缓存机制，提高频繁访问数据的响应速度。
7. 对敏感信息进行加密存储，确保用户隐私安全。
8. 实现日志记录系统，便于问题排查和系统优化。
9. 考虑实现限流机制，防止API被滥用。
10. 定期进行数据备份，制定数据恢复策略。
11. 确保系统的可扩展性，以应对用户量增长和新功能的添加。
12. 实现完善的错误处理和异常报告机制，提高系统的可靠性。
13. 考虑国际化和本地化，为将来的多语言支持做准备。
14. 实现用户反馈机制，收集用户使用过程中的问题和建议。
15. 定期进行安全审计和渗透测试，及时修复潜在的安全漏洞。