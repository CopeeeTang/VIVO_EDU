# 项目结构

family_education_ai/
├── app/
│   ├── __init__.py
│   ├── models.py
│   ├── auth.py
│   ├── celery_config.py
│   ├── extensions.py
│   ├── schemas.py
│   ├── utils/
│   │   ├── storage.py
│   │   ├── tasks.py
│   │   ├── final_generate.py
│   │   ├── prompt.py
│   │   ├── request_api.py
│   │   ├── script_analysis.py
│   │   └── tx.py
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── auth_routes.py
│   │   ├── audio_routes.py
│   │   ├── analysis_routes.py
│   │   └── main_routes.py
├── checkpoints/
├── Keda_api/
├── migrations/
├── uploads/
├── static/
│   ├── css/
│   ├── js/
│   ├── pic/
│   └── transcripts/
├── templates/
│   ├── index.html
│   └── report.html
│   ├── login.html
│   ├── register.html
│   ├── upload.html
├── config.py
├── celery_worker.py
├── requirements.txt
└── run.py

graph TD
    subgraph Client-Side
        UI_Auth[用户认证界面] --> |用户登录/注册| Client_API_Send
        UI_Upload[音频上传/录制界面] --> |上传/录制音频| Client_API_Send
        Client_API_Send -- HTTP Request --> Server_API_Receive[服务器API入口]
        Server_API_Send[服务器API出口] -- HTTP Response --> UI_Display[结果/状态展示界面]
        UI_Display --> |用户查看| User([用户])
        User --> UI_Auth
        User --> UI_Upload
    end

    subgraph Server-Side
        Server_API_Receive --> Store_Audio[1. 存储音频文件]
        Server_API_Receive --> |用户信息| DB[(数据库: 用户画像, 分析结果)]
        Store_Audio --> Create_Task[2. 创建异步任务]
        Create_Task --> Task_Queue([Celery/Redis 任务队列])

        Task_Queue --> Worker[Celery Worker]
        Worker --> Audio_Pre[3. 音频预处理 (转写)]
        Audio_Pre --> |转录文本| Scene_Recon[4. 场景重建模块 (LLM + Prompts)]
        Scene_Recon --> |场景信息| Portrait_Model[5. 亲子画像建模模块 (LLM + 编码)]
        Scene_Recon --> |场景信息| RAG_Query[6a. 生成RAG查询]
        Portrait_Model --> |画像信息| RAG_Query
        Portrait_Model --> |更新画像| DB

        RAG_Query --> Knowledge_Base_Retrieval[6b. 教育知识库检索 (RAG)]
        Knowledge_Base[(知识库: P.E.T., 案例)] --> Knowledge_Base_Retrieval
        Knowledge_Base_Retrieval --> |相关知识片段| Strategy_Gen[7. P.E.T.策略生成模块 (LLM)]
        Scene_Recon --> |场景信息| Strategy_Gen
        Portrait_Model --> |画像信息| Strategy_Gen

        Strategy_Gen --> |策略报告| Store_Result[8. 存储分析结果]
        Store_Result --> DB
        Store_Result --> Server_API_Send
    end

    style Client-Side fill:#ddeeff,stroke:#333,stroke-width:2px
    style Server-Side fill:#eeffee,stroke:#333,stroke-width:2px
    style DB fill:#f9f,stroke:#333,stroke-width:2px
    style Knowledge_Base fill:#ffc,stroke:#333,stroke-width:2px
    style Task_Queue fill:#fcc,stroke:#333,stroke-width:2px

# 项目根目录 (family_education_ai/)

这是整个项目的根目录，包含所有子目录和文件。主要包含以下部分：

## 1. app/

`app/` 目录是项目的核心部分，包含了Flask应用的所有代码。具体结构如下：

### 1.1 __init__.py

**功能**：这是 app 包的初始化脚本。它负责创建和配置Flask应用实例，初始化各种扩展，并注册所有的蓝图。

**主要任务**：

- 创建和配置 Flask 应用实例
- 初始化数据库(db)、数据库迁移(migrate)、JWT认证(jwt)和CORS跨域(cors)等扩展
- 配置 JWT 相关参数(过期时间、token位置等)
- 配置日志系统
- 测试 Redis 连接
- 注册认证(auth)、音频(audio)、分析(analysis)和主页(main)蓝图
- 初始化 Celery
- 创建上传文件夹和转录文件夹
- 初始化数据库表
- 配置错误处理(404、500)和静态文件路由

### 1.2 models.py 

**功能**：定义数据库模型。使用SQLAlchemy ORM定义所有数据表和关系。

**主要模型**：

- User: 用户模型
  - user_id: 主键
  - username: 用户名(唯一)
  - password: 密码
  - created_at: 创建时间
  - last_login: 最后登录时间

- AudioFile: 音频文件模型
  - file_id: UUID主键
  - user_id: 外键关联用户
  - file_name: 文件名
  - file_path: 文件路径
  - duration: 时长
  - status: 处理状态(pending/processing/completed/failed)
  - progress: 处理进度
  - task_id: 任务ID
  
- Transcription: 转录模型
  - transcription_id: 主键
  - file_id: 关联音频文件
  - transcript/content: 转录内容

- AnalysisResult: 分析结果模型
  - analysis_id: 主键
  - file_id: 关联音频文件
  - 包含家庭概况、对话分析、情感分析等多个分析结果字段

### 1.3 auth.py

**功能**：提供用户认证相关的路由和功能。

**主要功能**：

- /register 注册路由:
  - 验证用户名密码
  - 检查用户名是否存在
  - 创建新用户

- /login 登录路由:
  - 验证用户名密码
  - 生成JWT访问令牌
  - 更新最后登录时间

注: 此文件功能与 auth_routes.py 重复,建议合并到 auth_routes.py 中。

### 1.4 utils/

`utils/` 目录存放项目中使用的各种辅助工具和模块，提升代码的复用性和组织性。

#### 1.4.1 storage.py

**功能**：处理与文件存储相关的操作，例如文件上传、下载、删除等。

**主要任务**：

- 定义上传文件到服务器或云存储的函数。
- 定义从存储中下载或删除文件的函数。
- 加载和管理知识库文档。

#### 1.4.2 tasks.py

**功能**：定义异步任务，处理音频文件的分析流程。

**主要任务**：

- process_audio_task: 处理音频文件的分析任务
  - 下载音频文件
  - 转写音频为文本
  - 保存转写结果到JSON文件和数据库
  - 加载知识库数据
  - 生成干预策略
  - 保存分析结果到数据库
  - 更新任务状态和进度

#### 1.4.3 final_generate.py

**功能**：实现音频分析和干预策略生成的核心功能。

**主要任务**：

- record_to_transcript: 音频转录为文本
- generate_intervention_strategy: 生成干预策略
  - 生成家庭概况
  - 对话分析
  - 情感分析
  - 反思问题
  - 冲突分析
  - 行为分析
  - 策略生成

#### 1.4.4 request_api.py

**功能**：封装与外部API的交互。

**主要任务**：

- 集成OpenAI API
- 处理音频文件
- 管理API请求和响应

#### 1.4.5 tx.py

**功能**：实现场景重建和知识库检索功能。

**主要类**：

- Scenery_rebuild: 分析亲子对话重建家庭教育场景
  - 场景数量识别
  - 场景摘要生成
  - 关键细节提取
  
- Knowledge_Base: 知识库检索和策略生成
  - 多样化查询生成
  - 策略链生成
  - 案例生成
  - 对话改善
  - 问题分解

#### 1.4.6 prompt.py

**功能**：定义各类提示模板。

**主要任务**：

- 生成用户画像提示
- 场景构建提示
- 家庭概况生成
- 对话分析提示
- 情感分析提示
- 反思问题生成
- 冲突分析提示
- 行为分析提示
- 策略生成提示

### 1.5 routes/

`routes/` 目录包含了应用的所有路由文件，按功能模块化组织，便于管理和扩展。

#### 1.5.1 __init__.py

**功能**：将 routes 目录标记为Python包，使其内部模块可以被导入和使用。

#### 1.5.2 auth_routes.py

**功能**：管理与用户认证相关的路由，如注册、登录、登出等。

**主要任务**：

- 用户注册路由（/register）：处理新用户注册
- 用户登录路由（/login）：验证用户身份并生成JWT令牌
- 令牌刷新路由（/refresh）：刷新访问令牌
- 登出路由（/logout）：用户退出登录
- 登录检查路由（/check_login）：验证用户登录状态
- 受保护路由示例（/protected）：展示JWT保护的路由

#### 1.5.3 audio_routes.py

**功能**：管理与音频文件相关的路由，如上传、列出、删除音频文件等。

**主要任务**：

- 获取上传URL（/get_upload_url）：生成文件上传URL
- 文件上传（/upload/<file_id>）：处理音频文件上传
- 上传完成确认（/upload-complete）：确认文件上传完成
- 启动分析（/analyze/<file_id>）：开始音频分析任务
- 获取进度（/progress/<file_id>）：查询分析进度
- 文件列表（/list_files）：获取用户的音频文件列表
- 文件删除（/delete/<file_id>）：删除音频文件
- 文件下载（/download/<file_id>）：下载音频文件
- 分析示例（/analyze/<filename>）：获取分析示例结果

#### 1.5.4 analysis_routes.py

**功能**：管理与分析结果相关的路由，如获取分析报告、查看分析状态等。

**主要任务**：

- 获取分析结果（/result/<file_id>）：获取完整分析报告
- 获取处理状态（/status/<file_id>）：查询文件处理状态
- 获取历史记录（/history）：查看历史分析记录
- 获取趋势数据（/trend）：分析数据趋势统计
- 支持用户报告生成、分析摘要和结果比较功能

#### 1.5.5 main_routes.py

**功能**：管理主页面和报告页的路由，如渲染首页、报告页面等。

**主要任务**：

- 根路由（/）：重定向到登录页面
- 登录页面（/login）：渲染登录页面
- 注册页面（/register）：渲染注册页面
- 上传页面（/upload）：渲染录音上传管理页面
- 报告页面（/report/<file_id>）：渲染分析报告页面，展示分析结果

### 1.6 extensions.py

**功能**：集中初始化和管理Flask扩展，提高代码的模块化和可维护性。

**主要任务**：

- 初始化SQLAlchemy、Flask-Migrate、Flask-JWT-Extended、Flask-CORS等扩展。
- 提供扩展实例供项目其他部分导入和使用。

## 2. migrations/

**功能**：存放数据库迁移脚本，使用Flask-Migrate（基于Alembic）来管理数据库的版本控制。

**主要任务**：

- 记录数据库模型的变更历史。
- 支持数据库结构的升级和降级操作。

## 3. uploads/

**功能**：存储用户上传的音频文件。

**主要任务**：

- 保存用户上传的 .wav、.mp3、.m4a 等音频文件。
- 管理文件的存储结构和权限，确保文件的安全性和可访问性。

## 4. static/

**功能**：存放静态文件，如CSS、JavaScript、图片等资源，供前端页面使用。

**主要任务**：

- 存储和提供前端所需的静态资源。
- 提供 transcripts/ 子目录，用于存储转录后的文本文件。

### 4.1 transcripts/

**功能**：存储音频文件的转录文本或相关分析文件。

**主要任务**：

- 保存转换后的文本文件，供后续分析或用户查看使用。
- 管理文件的命名规范和存储路径。

## 5. templates/

**功能**：存放Jinja2模板文件，用于渲染动态HTML页面。

**主要任务**：

- 提供前端页面的模板，如首页和报告页。
- 实现页面的动态内容展示和数据绑定。

### 5.1 index.html

**功能**：应用的主页，通常包含用户注册、登录、音频上传、文件列表等功能的界面。

**主要任务**：

- 提供用户交互的界面元素，如表单、按钮、表格等。
- 通过Jinja2模板语法嵌入动态数据。
- 引入前端的CSS和JavaScript文件。

### 5.2 report.html

**功能**：展示分析结果的页面，供用户查看各类分析报告。

**主要任务**：

- 显示用户上传音频文件的分析结果，如家庭教育风格、沟通分析、情绪分析等。
- 提供友好的数据展示方式，如文本、图表等。
- 引入前端的CSS和JavaScript文件，提升用户体验。

## 6. config.py

**功能**：管理项目的配置信息，如数据库连接URI、秘钥、文件存储路径等。

**主要任务**：

- 定义Flask应用的配置类（如Config）。
- 设置环境变量读取优先级，确保敏感信息的安全管理。
- 配置SQLAlchemy、JWT、Celery等扩展的参数。

## 7. requirements.txt

**功能**：列出项目所需的所有Python依赖包及其版本，方便环境的快速搭建和依赖管理。

**主要任务**：

- 列出所有使用的第三方库，如Flask、SQLAlchemy、Flask-JWT-Extended、Celery、PyMySQL等。
- 明确指定版本，确保开发和生产环境的一致性。

## 8. run.py

**功能**：Flask应用的入口点，用于启动开发服务器。

**主要任务**：

- 导入并创建Flask应用实例。
- 配置应用的运行参数（如调试模式、host和port）。
- 启动Flask内置的开发服务器，供本地开发和测试使用。

## 总结

这个项目结构遵循了Flask应用的最佳实践，模块化地组织了不同功能的代码，提升了代码的可维护性和可扩展性。具体来说：

- `app/`：核心应用代码，包含模型、路由、工具函数等。
- `migrations/`：数据库迁移脚本，管理数据库版本。
- `uploads/`：用户上传音频文件的存储目录。
- `static/`：静态资源存放目录，支持前端页面的展示。
- `templates/`：Jinja2模板文件，渲染动态HTML页面。
- `config.py`：项目配置管理，集中处理应用的各项配置。
- `requirements.txt`：依赖列表，便于环境搭建和部署。
- `run.py`：应用启动脚本，启动Flask开发服务器。